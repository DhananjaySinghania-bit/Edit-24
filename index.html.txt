<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Dhananjay - Personal Website</title>

<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />

<style>
  :root{
    --blue1:#1e90ff; --blue2:#00bfff; --card:#e0f7fa; --accent:#00796b;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  body{font-family:'Roboto',sans-serif;background:linear-gradient(180deg,#f0f8ff,#e0f7fa);color:#222;overflow-x:hidden;}

  /* SPLASH */
  #splash{
    position:fixed;inset:0;
    background:linear-gradient(135deg,var(--blue1),var(--blue2));
    display:flex;flex-direction:column;align-items:center;justify-content:center;
    color:#fff;font-weight:700;font-size:2.2rem;z-index:9999;transition:opacity .45s ease;
  }
  #splash .sub{font-size:1rem;font-weight:400;margin-top:.5rem;opacity:.95}

  /* Layout */
  header{background:linear-gradient(90deg,var(--blue1),var(--blue2));color:#fff;padding:44px 18px;text-align:center;box-shadow:0 6px 18px rgba(0,0,0,.12);opacity:0;transform:translateY(30px);transition:all .6s ease}
  header.visible{opacity:1;transform:none}
  header h1{font-size:2.4rem;letter-spacing:1px}
  header p{margin-top:8px;font-size:1.05rem;color:#f0f8ff}

  main{max-width:980px;margin:26px auto;padding:0 18px}

  section{padding:40px 0;text-align:center;opacity:0;transform:translateY(30px);transition:all .6s ease}
  section.visible{opacity:1;transform:none}
  section h2{color:var(--blue1);margin-bottom:14px;font-size:1.6rem}

  .avatar{width:170px;height:170px;border-radius:50%;object-fit:cover;border:4px solid var(--blue1);display:block;margin:0 auto 12px;transition:transform .4s,box-shadow .4s}
  .avatar:hover{transform:scale(1.06);box-shadow:0 10px 26px rgba(0,0,0,.12)}

  .stack{display:flex;flex-direction:column;gap:12px;align-items:center}
  .card{width:280px;background:linear-gradient(135deg,#fff,var(--card));padding:16px;border-radius:12px;box-shadow:0 10px 30px rgba(0,0,0,.06);display:flex;flex-direction:column;align-items:center;justify-content:center;font-size:1rem;transition:transform .35s,box-shadow .35s}
  .card i{font-size:1.2rem;color:var(--blue1);margin-bottom:8px}
  .card:hover{transform:translateY(-8px);box-shadow:0 18px 40px rgba(0,0,0,.12)}

  .note{max-width:920px;margin:0 auto;background:var(--card);padding:18px;border-radius:12px;color:#034;box-shadow:0 14px 36px rgba(0,0,0,.06);text-align:left;white-space:pre-line;line-height:1.6}

  .buttons{display:flex;gap:18px;justify-content:center;margin-top:20px;opacity:0;transform:translateY(20px);transition:all .6s ease}
  .buttons.visible{opacity:1;transform:none}
  .btn{width:62px;height:62px;border-radius:50%;display:inline-flex;align-items:center;justify-content:center;color:#fff;text-decoration:none;font-size:1.25rem;box-shadow:0 10px 24px rgba(0,0,0,.08);transition:transform .25s,box-shadow .25s}
  .btn:hover{transform:scale(1.12);box-shadow:0 18px 40px rgba(0,0,0,.12)}
  .email{background:#D44638}.phone{background:#25D366}
  .instagram{background:linear-gradient(45deg,#f09433,#e6683c,#dc2743,#cc2366,#bc1888)}

  footer{margin-top:32px;padding:18px;text-align:center;background:linear-gradient(90deg,var(--blue1),var(--blue2));color:#fff;border-radius:8px;opacity:0;transform:translateY(20px);transition:all .6s ease}
  footer.visible{opacity:1;transform:none}

  @media(max-width:640px){
    header h1{font-size:1.8rem}
    .card{width:94%}
    .avatar{width:150px;height:150px}
  }
</style>
</head>
<body>

  <!-- Splash -->
  <div id="splash" role="status" aria-live="polite">
    You are in my World
    <div class="sub">Welcome</div>
  </div>

  <!-- Content -->
  <header id="siteHeader" aria-hidden="true">
    <h1>Dhananjay</h1>
    <p>Empowering Change through Knowledge and Action</p>
  </header>

  <main id="main" aria-hidden="true">
    <section id="about" aria-labelledby="aboutH">
      <img class="avatar" src="https://i.ibb.co/bMZ8PznK/cropped-circle-image-6.png" alt="Dhananjay">
      <h2 id="aboutH">About Me</h2>
      <p style="max-width:820px;margin:0 auto;color:#234;">Hi! I'm Dhananjay, passionate about technology, education, and social reform. I strive to create impactful solutions that bridge gaps and foster inclusive growth.</p>
    </section>

    <section id="skills" aria-labelledby="skillsH">
      <h2 id="skillsH">Skills</h2>
      <div class="stack" role="list">
        <div class="card" role="listitem"><i class="fas fa-project-diagram"></i>Architecting</div>
        <div class="card" role="listitem"><i class="fab fa-html5"></i>HTML &amp; CSS</div>
        <div class="card" role="listitem"><i class="fas fa-robot"></i>AI Based Work</div>
        <div class="card" role="listitem"><i class="fas fa-paint-brush"></i>Graphics Designer</div>
        <div class="card" role="listitem">More...</div>
      </div>
    </section>

    <section id="projects" aria-labelledby="projectsH">
      <h2 id="projectsH">Projects</h2>
      <div class="stack">
        <div class="card">App building Ongoing ⏸️</div>
        <div class="card">Website ✅</div>
        <div class="card">Business card &amp; logo ✅</div>
        <div class="card">More…</div>
      </div>
    </section>

    <section id="note" aria-labelledby="noteH">
      <h2 id="noteH">Note</h2>
      <div class="note">
&gt; Hello!  
If you truly want to become successful, you must apply the (wwwH)-rule in your life. Without it, success will remain out of reach. People may misunderstand you, no matter which field you are in—whether it’s academics, your career, or any other area.

Remember, success isn’t measured by how much money you have. True success comes when your actions inspire others or improve lives because of you.

This (wwwH)-rule is unique and cannot be found on the internet or anywhere else—it has been created by me personally to help people.

To understand what the (wwwH)-rule is and how to apply it in your daily life, contact us.👇🏼
      </div>
    </section>

    <div class="buttons" role="navigation" aria-label="contacts">
      <a class="btn email" href="mailto:iimmenseaiexperts@gmail.com" title="Email"><i class="fas fa-envelope"></i></a>
      <a class="btn phone" href="tel:+919219671761" title="Phone"><i class="fas fa-phone"></i></a>
      <a class="btn instagram" href="https://www.instagram.com/cosmos_reformist" target="_blank" rel="noopener" title="Instagram"><i class="fab fa-instagram"></i></a>
    </div>

    <footer id="siteFooter" aria-hidden="true">
      &copy; 2025 Dhananjay. All rights reserved.
    </footer>
  </main>

<script>
(function(){
  const splash = document.getElementById('splash');
  const header = document.getElementById('siteHeader');
  const main = document.getElementById('main');
  const sections = main.querySelectorAll('section');
  const buttons = document.querySelector('.buttons');
  const footer = document.getElementById('siteFooter');

  function revealAllImmediate(){
    header.classList.add('visible'); header.removeAttribute('aria-hidden');
    sections.forEach(s=>s.classList.add('visible'));
    buttons.classList.add('visible');
    footer.classList.add('visible'); footer.removeAttribute('aria-hidden');
    if(splash && splash.parentNode) splash.parentNode.removeChild(splash);
  }

  window.addEventListener('load', ()=>{
    const minShow = 1500;
    setTimeout(()=>{
      if(splash){ splash.style.opacity='0'; }
      setTimeout(()=>{
        if(splash && splash.parentNode) splash.parentNode.removeChild(splash);
        header.classList.add('visible'); header.removeAttribute('aria-hidden');
        sections.forEach((s,i)=> setTimeout(()=> s.classList.add('visible'), i*120));
        setTimeout(()=> buttons.classList.add('visible'), sections.length*120 + 150);
        setTimeout(()=> { footer.classList.add('visible'); footer.removeAttribute('aria-hidden'); }, sections.length*120 + 300);
        main.removeAttribute('aria-hidden');
      },480);
    },minShow);
  });

  setTimeout(()=> revealAllImmediate(), 4000);

  function onScrollCheck(){
    const trigger = window.innerHeight * 0.85;
    sections.forEach(s=>{
      if(s.classList.contains('visible')) return;
      if(s.getBoundingClientRect().top < trigger) s.classList.add('visible');
    });
  }
  window.addEventListener('scroll', onScrollCheck);
  window.addEventListener('resize', onScrollCheck);
})();
</script>

</body>
</html>